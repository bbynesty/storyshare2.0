[phases.setup]
nixPkgs = ["gcc", "cmake", "git", "boost", "openssl", "pkg-config"]

[phases.build]
cmds = [
  "cd backend",
  "mkdir -p build",
  "cd build",
  "cmake .. -DCMAKE_BUILD_TYPE=Release",
  "cmake --build . --config Release"
]

[start]
cmd = "cd backend/build && ./server"

